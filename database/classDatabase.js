/**
 * classDatabase.js
 * ─────────────────────────────────────────────────────────────
 * Re-export del database delle classi.
 * 
 * Questo file mantiene la compatibilità con tutti gli import
 * esistenti nel progetto che puntano a questo percorso.
 * 
 * La struttura modulare è in ./classes/
 * 
 * @author DM Tool
 */

// Re-export per mantenere compatibilità
export { classDatabase } from './classes/index.js';